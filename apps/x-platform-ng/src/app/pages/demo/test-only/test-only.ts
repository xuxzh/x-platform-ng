import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-test-only',
  imports: [CommonModule],
  templateUrl: './test-only.html',
  styleUrl: './test-only.less',
})
export class TestOnly {}
